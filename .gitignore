# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets — never commit
firebase-service-account.json
.env
.env.*

# Private / personal data exports — never publish (contain PII)
responses.csv

# Local tooling
.claude/

# Internal planning / security analysis — keep OUT of the public repo
/20260614 Cleanup Workplan/
docs/ARCHITECTURE.md
docs/DEPENDENCIES.md
docs/PROPOSAL.md

# Local data snapshots (Phase 4 rollback) — never commit
_backups/

# Raw source blocks (113MB) — regenerate pipeline locally; never commit
pipeline-data/manzanas.geojson
